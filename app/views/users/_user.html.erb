<h2>User info</h2>
     <%= attachment_image_tag user, :profile_image, :fill, 63, 63, format: 'jpeg', fallback: "no_image-72379a4da21cb04a58337cf92d85bc39f8d534cbc78254c2085acaca82236bfa.jpg" %>
     <table class="table">
      <tbody>
       <tr></tr>
       <tr>
        <th>name</th>
        <th><%= user.name %></th>
       </tr>
       <tr>
        <th>introduction</th>
        <th><%= user.introduction %></th>
       </tr>
        <tr><% if user.id == current_user.id %></tr>
        <% end %>
      </tbody>
     </table>
     <div class="row"><%= link_to edit_user_path(user), class:"btn btn-outline-secondary btn-block" do %>
     <i class="fas fa-user-cog"></i>
     <% end %>
     </div>